-- ====================================
-- HZCU VIBECODING 课程实践项目
-- 数据库建表脚本
-- 请在此文件编写你的建表语句
-- 至少 2 张表
-- ====================================

-- 创建数据库（如有需要，按实际项目命名）
-- CREATE DATABASE your_project DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- USE your_project;

-- ====================================
-- 表 1：示例 - 请替换为你的实际表
-- ====================================
-- CREATE TABLE table_name (
--     id BIGINT PRIMARY KEY AUTO_INCREMENT,
--     name VARCHAR(100) NOT NULL,
--     created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
-- );

-- ====================================
-- 表 2：示例 - 请替换为你的实际表
-- ====================================
-- CREATE TABLE related_table (
--     id BIGINT PRIMARY KEY AUTO_INCREMENT,
--     table_name_id BIGINT NOT NULL,
--     description VARCHAR(500),
--     created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
--     CONSTRAINT fk_example FOREIGN KEY (table_name_id) REFERENCES table_name(id)
-- );
